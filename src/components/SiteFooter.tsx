export function SiteFooter() {
  return (
    <footer className="mt-24 border-t border-rule">
      <div className="mx-auto max-w-6xl px-4 py-8 text-sm text-ink-faint sm:px-8">
        <p className="max-w-prose">
          Page prose is AI-drafted with citations attached, then reviewed. Where sources disagree,
          the disagreement is stated rather than settled. Open-problem status is recorded as of
          writing, not tracked live. Corrections are made by editing the content files in the
          repository.
        </p>
      </div>
    </footer>
  );
}
